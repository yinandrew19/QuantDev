*.swp
package-lock.json
__pycache__
.pytest_cache
.venv
*.egg-info

# ignore local stack docker image
.localstack

# CDK asset staging directory
.cdk.staging
cdk.out

# macOS metadata
.DS_Store

# Lambda layer is built from requirements_layer; never commit installed wheels
lambda_layer/python/

# Secrets / local env files
.env
.env.*
!.env.example
